spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
        git:
          clone-on-start: true
          uri: https://github.com/silasmahler/kotlin-microservices-config
        searchPaths: config-service/src/main/resources/shared
