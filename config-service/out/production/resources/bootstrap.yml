spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
        git:
          clone-on-start: true
          uri: https://github.com/silasmahler/kotlin-microservices
        searchPaths: config-service/src/main/resources/shared
